# public/

Vite serves files in `public/` at the site root.

Example:
- Put an image at `public/images/bangkok.jpg`
- Reference it as `/images/bangkok.jpg` (not `./public/...`)

City images for the app live in `public/images/city/` and are referenced as `/images/city/<file>.jpg`.
